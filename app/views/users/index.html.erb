<h1>Users</h1>
<% @users.each do |user| %>
  <div>
      <div><%= image_tag(user.photo, width: 50) %></div>
      <%= link_to(user_path(user)) do %>
      <div><%= user.name %></div>
      <% end %>
      <div>Number of posts: <%= user.posts_counter %></div>
  </div>
<% end %>
